apiVersion: v1
kind: Service
metadata:
  name: bankapp-service
  namespace: bankapp
  labels:
    app: bankapp
spec:
  type: ClusterIP
  selector:
    app: bankapp
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    # nodePort: 31125
    
